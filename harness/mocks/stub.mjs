// Benign default-export stub for native modules imported (as a default) but never exercised on the
// deterministic path — e.g. @kingstinct/react-native-healthkit in claude.ts, expo-location.
export default {};
